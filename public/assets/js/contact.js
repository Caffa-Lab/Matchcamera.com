const $=s=>document.querySelector(s);$('#contactForm').addEventListener('submit',e=>{e.preventDefault();const name=$('#contactName').value.trim(),email=$('#contactEmail').value.trim(),type=$('#contactType').value,subject=$('#contactSubject').value.trim(),message=$('#contactMessage').value.trim();const body=`문의 유형: ${type}\n이름: ${name}\n회신 이메일: ${email}\n\n${message}`;location.href=`mailto:admin@matchcamera.com?subject=${encodeURIComponent(`[Matchcamera 문의] ${subject}`)}&body=${encodeURIComponent(body)}`;});
